# https://docs.gunicorn.org/en/stable/deploy.html#supervisor

[program:django_wsgi]
# TCP socket used by Nginx backend upstream
socket=tcp://localhost:8080

# Directory where your site's project files are located
directory=/code

# Each process needs to have a separate socket file, so we use process_num
# Make sure to update "mysite.asgi" to match your project name
command=/usr/local/bin/gunicorn config.wsgi:application --bind unix:/run/gunicorn.sock --config ./gunicorn.conf.py

# Number of processes to startup, roughly the number of CPUs you have
numprocs=2

# Give each process a unique name so they can be told apart
process_name=asgi%(process_num)d

# Automatically start and recover processes
autostart=true
autorestart=true

# Choose where you want your log to go
stdout_logfile=/code/logs/wsgi.log
redirect_stderr=true